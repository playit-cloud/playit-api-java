/*
 * playit.gg's api
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: unstable
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package gg.playit.api.models;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import gg.playit.api.models.AccountTunnelAllocationOneOf;
import gg.playit.api.models.AccountTunnelAllocationOneOf1;
import gg.playit.api.models.TunnelAllocated;
import java.io.IOException;
import java.util.Arrays;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import gg.playit.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen")
public class AccountTunnelAllocation extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(AccountTunnelAllocation.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!AccountTunnelAllocation.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'AccountTunnelAllocation' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<AccountTunnelAllocationOneOf> adapterAccountTunnelAllocationOneOf = gson.getDelegateAdapter(this, TypeToken.get(AccountTunnelAllocationOneOf.class));
            final TypeAdapter<AccountTunnelAllocationOneOf1> adapterAccountTunnelAllocationOneOf1 = gson.getDelegateAdapter(this, TypeToken.get(AccountTunnelAllocationOneOf1.class));

            return (TypeAdapter<T>) new TypeAdapter<AccountTunnelAllocation>() {
                @Override
                public void write(JsonWriter out, AccountTunnelAllocation value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `AccountTunnelAllocationOneOf`
                    if (value.getActualInstance() instanceof AccountTunnelAllocationOneOf) {
                      JsonElement element = adapterAccountTunnelAllocationOneOf.toJsonTree((AccountTunnelAllocationOneOf)value.getActualInstance());
                      elementAdapter.write(out, element);
                      return;
                    }
                    // check if the actual instance is of the type `AccountTunnelAllocationOneOf1`
                    if (value.getActualInstance() instanceof AccountTunnelAllocationOneOf1) {
                      JsonElement element = adapterAccountTunnelAllocationOneOf1.toJsonTree((AccountTunnelAllocationOneOf1)value.getActualInstance());
                      elementAdapter.write(out, element);
                      return;
                    }
                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: AccountTunnelAllocationOneOf, AccountTunnelAllocationOneOf1");
                }

                @Override
                public AccountTunnelAllocation read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize AccountTunnelAllocationOneOf
                    try {
                      // validate the JSON object to see if any exception is thrown
                      AccountTunnelAllocationOneOf.validateJsonElement(jsonElement);
                      actualAdapter = adapterAccountTunnelAllocationOneOf;
                      match++;
                      log.log(Level.FINER, "Input data matches schema 'AccountTunnelAllocationOneOf'");
                    } catch (Exception e) {
                      // deserialization failed, continue
                      errorMessages.add(String.format("Deserialization for AccountTunnelAllocationOneOf failed with `%s`.", e.getMessage()));
                      log.log(Level.FINER, "Input data does not match schema 'AccountTunnelAllocationOneOf'", e);
                    }
                    // deserialize AccountTunnelAllocationOneOf1
                    try {
                      // validate the JSON object to see if any exception is thrown
                      AccountTunnelAllocationOneOf1.validateJsonElement(jsonElement);
                      actualAdapter = adapterAccountTunnelAllocationOneOf1;
                      match++;
                      log.log(Level.FINER, "Input data matches schema 'AccountTunnelAllocationOneOf1'");
                    } catch (Exception e) {
                      // deserialization failed, continue
                      errorMessages.add(String.format("Deserialization for AccountTunnelAllocationOneOf1 failed with `%s`.", e.getMessage()));
                      log.log(Level.FINER, "Input data does not match schema 'AccountTunnelAllocationOneOf1'", e);
                    }

                    if (match == 1) {
                        AccountTunnelAllocation ret = new AccountTunnelAllocation();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    }

                    throw new IOException(String.format("Failed deserialization for AccountTunnelAllocation: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public AccountTunnelAllocation() {
        super("oneOf", Boolean.FALSE);
    }

    public AccountTunnelAllocation(AccountTunnelAllocationOneOf o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    public AccountTunnelAllocation(AccountTunnelAllocationOneOf1 o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("AccountTunnelAllocationOneOf", AccountTunnelAllocationOneOf.class);
        schemas.put("AccountTunnelAllocationOneOf1", AccountTunnelAllocationOneOf1.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return AccountTunnelAllocation.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * AccountTunnelAllocationOneOf, AccountTunnelAllocationOneOf1
     *
     * It could be an instance of the 'oneOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof AccountTunnelAllocationOneOf) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof AccountTunnelAllocationOneOf1) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be AccountTunnelAllocationOneOf, AccountTunnelAllocationOneOf1");
    }

    /**
     * Get the actual instance, which can be the following:
     * AccountTunnelAllocationOneOf, AccountTunnelAllocationOneOf1
     *
     * @return The actual instance (AccountTunnelAllocationOneOf, AccountTunnelAllocationOneOf1)
     */
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `AccountTunnelAllocationOneOf`. If the actual instance is not `AccountTunnelAllocationOneOf`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `AccountTunnelAllocationOneOf`
     * @throws ClassCastException if the instance is not `AccountTunnelAllocationOneOf`
     */
    public AccountTunnelAllocationOneOf getAccountTunnelAllocationOneOf() throws ClassCastException {
        return (AccountTunnelAllocationOneOf)super.getActualInstance();
    }
    /**
     * Get the actual instance of `AccountTunnelAllocationOneOf1`. If the actual instance is not `AccountTunnelAllocationOneOf1`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `AccountTunnelAllocationOneOf1`
     * @throws ClassCastException if the instance is not `AccountTunnelAllocationOneOf1`
     */
    public AccountTunnelAllocationOneOf1 getAccountTunnelAllocationOneOf1() throws ClassCastException {
        return (AccountTunnelAllocationOneOf1)super.getActualInstance();
    }

 /**
  * Validates the JSON Element and throws an exception if issues found
  *
  * @param jsonElement JSON Element
  * @throws IOException if the JSON Element is invalid with respect to AccountTunnelAllocation
  */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
    // validate oneOf schemas one by one
    int validCount = 0;
    ArrayList<String> errorMessages = new ArrayList<>();
    // validate the json string with AccountTunnelAllocationOneOf
    try {
      AccountTunnelAllocationOneOf.validateJsonElement(jsonElement);
      validCount++;
    } catch (Exception e) {
      errorMessages.add(String.format("Deserialization for AccountTunnelAllocationOneOf failed with `%s`.", e.getMessage()));
      // continue to the next one
    }
    // validate the json string with AccountTunnelAllocationOneOf1
    try {
      AccountTunnelAllocationOneOf1.validateJsonElement(jsonElement);
      validCount++;
    } catch (Exception e) {
      errorMessages.add(String.format("Deserialization for AccountTunnelAllocationOneOf1 failed with `%s`.", e.getMessage()));
      // continue to the next one
    }
    if (validCount != 1) {
      throw new IOException(String.format("The JSON string is invalid for AccountTunnelAllocation with oneOf schemas: AccountTunnelAllocationOneOf, AccountTunnelAllocationOneOf1. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonElement.toString()));
    }
  }

 /**
  * Create an instance of AccountTunnelAllocation given an JSON string
  *
  * @param jsonString JSON string
  * @return An instance of AccountTunnelAllocation
  * @throws IOException if the JSON string is invalid with respect to AccountTunnelAllocation
  */
  public static AccountTunnelAllocation fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, AccountTunnelAllocation.class);
  }

 /**
  * Convert an instance of AccountTunnelAllocation to an JSON string
  *
  * @return JSON string
  */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

